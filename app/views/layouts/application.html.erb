<!DOCTYPE html>
<html>
  <head>
    <title>
      <% if content_for(:title) %>
        <%= yield :title %>
      <% else %>
        My Todo
      <% end %>
    </title>

    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "volt", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-200">
    <div class="container">
      <div class="col-sm-11 py-6 col-md-9 mx-auto">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
